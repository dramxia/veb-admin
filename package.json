{
  "name": "veb-monorepo",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=20.10.0"
  },
  "scripts": {
    "dev": "pnpm --parallel --filter @veb/web --filter @veb/veb-api --filter @veb/blog-api dev",
    "dev:infra": "docker compose --env-file .env.development up -d veb-postgres blog-postgres",
    "dev:all": "pnpm dev:infra && pnpm db:migrate:deploy && pnpm dev",
    "build": "pnpm -r --if-present build",
    "lint": "pnpm -r --if-present lint",
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test",
    "test:e2e": "pnpm --filter @veb/web test:e2e",
    "db:generate": "pnpm --filter @veb/veb-api prisma:generate && pnpm --filter @veb/blog-api prisma:generate",
    "db:migrate:dev:veb": "pnpm --filter @veb/veb-api prisma:migrate:dev",
    "db:migrate:dev:blog": "pnpm --filter @veb/blog-api prisma:migrate:dev",
    "db:migrate:deploy": "pnpm --filter @veb/veb-api prisma:migrate:deploy && pnpm --filter @veb/blog-api prisma:migrate:deploy",
    "db:migrate:deploy:production": "pnpm --filter @veb/veb-api prisma:migrate:deploy:production && pnpm --filter @veb/blog-api prisma:migrate:deploy:production",
    "db:seed:veb": "pnpm --filter @veb/veb-api db:seed",
    "db:seed:blog": "pnpm --filter @veb/blog-api db:seed",
    "db:seed": "pnpm db:seed:veb && pnpm db:seed:blog",
    "compose:up": "docker compose --env-file .env.development up --build",
    "compose:deploy": "./deploy/compose-deploy.sh",
    "prepare": "husky"
  },
  "devDependencies": {
    "@commitlint/cli": "^21.0.1",
    "@commitlint/config-conventional": "^21.0.1",
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx}": [
      "prettier --write"
    ],
    "*.{json,md,css,yml,yaml}": [
      "prettier --write"
    ]
  }
}
